{
  "name": "taskflow-mern",
  "private": true,
  "version": "1.0.0",
  "description": "Full stack MERN task management application",
  "scripts": {
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm start --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server"
  }
}
